#include <bits/stdc++.h>
#include <algorithm>
#include <deque>
#include <iterator>
#include <map>
#include <stack>
#include <unordered_map>

using namespace std;

int main()
{
    long long  n;
    long long  k;
    cin >> n >> k;
    map<long long ,long long > map;
    vector<long long > v(n);
    for (long long  i = 0; i < n; i++)
        cin >> v[i];
    

}